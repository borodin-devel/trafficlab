# Validation Study report status

No replacement Validation Study has been collected or published under the
corrected scientific-artifact schema. The prior Phase 7 result is historical
consistency evidence only: its raw run trees are absent and it predates the
required complete candidate bundle, separated fresh-simulation claim, genuine
held-out references, and clean-clone offline audit.

A future accepted report is generated from an exclusive, audited bundle at
`examples/validation_study/evidence/<study-id>/`. It must distinguish exactly
four claims: training selection, repeated-capture natural variation,
same-reference fresh simulation with the fixed final seed, and one genuine
independent held-out reference per workload. It must state the four mandatory
similarity components, configured aggregate weights, infeasible candidate
conditions, finite-sample limits, model limits, metric limits, and the scope of
generalization.
