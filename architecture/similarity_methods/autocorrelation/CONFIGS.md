# Autocorrelation Configuration

Required configuration is ordered nonempty distinct positive integer lags,
positive timing/frame feature weights with exact sum one, and one positive
exact-unit-sum weight per lag. Exact TOML syntax/defaults and maximum lag/count
resource bounds are unresolved and must precede implementation.
