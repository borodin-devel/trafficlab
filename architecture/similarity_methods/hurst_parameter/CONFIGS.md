# Hurst-Parameter Configuration

No settings/defaults are approved. Series construction, estimator, scale range,
regression/fit controls, minimum data, uncertainty, score scale, tolerance, and
resource limits are unresolved.
