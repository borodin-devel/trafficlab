# Wavelet-Scaling Configuration

No settings/defaults are approved. Series/horizon/sample interval, wavelet,
transform family, boundary/padding, levels, statistic, scale weights, distance,
score, tolerance, minimum data, and resource limits are unresolved.
