# Multi-Scale Rate Configuration

Required settings are finite positive comparison horizon, ordered nonempty
distinct positive widths, positive packet/original-byte feature weights with
exact sum one, and one positive exact-unit-sum scale weight per width. Exact
TOML paths/defaults and a maximum total-bin resource bound are unresolved and
must be approved before implementation.
