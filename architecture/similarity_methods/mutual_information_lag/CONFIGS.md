# Lagged Mutual-Information Configuration

No settings/defaults are approved. Feature variables/transforms, lags,
estimator and its bin/neighborhood controls, normalization, bias/uncertainty,
weights, score mapping, tolerance, minimum sample, and resource bounds are unresolved.
