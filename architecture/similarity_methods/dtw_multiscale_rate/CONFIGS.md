# Multi-Scale Rate DTW Configuration

No settings/defaults are approved. Horizon, widths, features, feature/scale
weights, local cost, warping constraints, path normalization, score mapping,
tolerance, and resource limits are unresolved.
