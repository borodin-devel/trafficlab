# Weighted L2 KS Configuration

Normal exact-decimal weights are `iat_weight = 0.5` and
`frame_size_weight = 0.5`. Each is finite/nonnegative and exact sum is one.
They are never normalized. Both component methods remain mandatory.
