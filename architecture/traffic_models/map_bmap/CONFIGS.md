# MAP/BMAP Configuration

No model-file schema or default is approved. Matrix dimensions/entries, batch
limits, marks, fitting controls, seed, stopping, and trainable fields are
unresolved; every current `map_bmap` model file is invalid.
