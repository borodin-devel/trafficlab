# Packet-Train Configuration

No schema/defaults exist. Train start/count, intratrain IAT, intertrain gap,
frame lengths, overlap, fitting, seed, stop, and trainable fields are unresolved.
Current model files are invalid.
