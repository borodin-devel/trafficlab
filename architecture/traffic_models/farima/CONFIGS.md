# FARIMA Configuration

No schema, fields, defaults, or overrides are approved. Any `farima` model file
is invalid in the current version. Research must define all parameters and
cross-constraints before registration.
