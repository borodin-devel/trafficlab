# State-Space Configuration

No schema/defaults exist. Latent dimension, transition/emission parameters,
observation domain, inference/fitting, regularization, seed, stopping,
generation, and trainable fields are unresolved. Current files are invalid.
