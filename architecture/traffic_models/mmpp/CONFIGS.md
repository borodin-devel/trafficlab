# MMPP Configuration

No fields or defaults are approved. State count, generator/transition values,
rates, initial law, marks, fitting, seed, stopping, and trainable fields are
unresolved. Any current `mmpp` model file is invalid.
